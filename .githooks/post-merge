#!/usr/bin/env bash

set -Eeuo pipefail

PROJECT_ROOT="$(git rev-parse --show-toplevel)"

echo "OakBoard: publishing the updated production build..."
/bin/bash "${PROJECT_ROOT}/scripts/deploy-production.sh"
